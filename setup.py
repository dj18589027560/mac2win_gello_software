import setuptools

setuptools.setup(
    name="gello",
    version="0.0.1",
    description="GELLO software (simplified for UR teleoperation)",
    packages=setuptools.find_packages(),
    python_requires=">=3.8",
)
